# Codex Instructions

- This Hugo/Docsy site is usually already served locally with watch mode. Do not run `hugo`, `hugo server`, `npm run build`, or other Hugo build commands after edits unless the user explicitly asks for a build or verification.
